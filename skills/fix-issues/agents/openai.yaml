interface:
  display_name: "Fix Issues"
  short_description: "Trace root causes, fix bugs, and verify regressions"
  default_prompt: "Use $fix-issues to reproduce or trace the bug, write a failing test first when reproduction steps are provided, fix the root cause, and verify related paths."
