interface:
  display_name: "Review Uncommitted"
  short_description: "Review uncommitted work with resumable cache sessions"
  default_prompt: "Use $review-uncommitted to review the current uncommitted changes, create a resumable cache session, and continue from that session in later passes."
